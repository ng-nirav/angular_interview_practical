import { IntersactionOnbserverDirective } from './intersaction-onbserver.directive';

describe('IntersactionOnbserverDirective', () => {
  it('should create an instance', () => {
    const directive = new IntersactionOnbserverDirective();
    expect(directive).toBeTruthy();
  });
});
